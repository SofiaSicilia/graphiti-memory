import type { SearchCandidate } from './search-config';
export interface VectorSearchOptions {
    limit: number;
    threshold: number;
    groupIds?: string[];
    entityTypes?: string[];
    excludeIds?: string[];
}
export declare class VectorSearchEngine {
    private supabase;
    /**
     * Search entities by vector similarity using pgvector
     */
    searchEntities(queryEmbedding: number[], options: VectorSearchOptions): Promise<SearchCandidate[]>;
    /**
     * Search edges by vector similarity on fact embeddings
     */
    searchEdges(queryEmbedding: number[], options: VectorSearchOptions): Promise<SearchCandidate[]>;
    /**
     * Search communities by vector similarity on community names/summaries
     */
    searchCommunities(_queryEmbedding: number[], options: VectorSearchOptions): Promise<SearchCandidate[]>;
    /**
     * Batch vector search across multiple content types
     */
    searchAll(queryEmbedding: number[], options: VectorSearchOptions & {
        includeEntities?: boolean;
        includeEdges?: boolean;
        includeCommunities?: boolean;
    }): Promise<SearchCandidate[]>;
}
//# sourceMappingURL=vector-search.d.ts.map