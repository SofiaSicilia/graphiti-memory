import type { SearchCandidate } from './search-config';
export interface FullTextSearchOptions {
    limit: number;
    groupIds?: string[];
    excludeIds?: string[];
}
export declare class FullTextSearchEngine {
    private supabase;
    /**
     * Full-text search on entities using PostgreSQL's built-in text search
     */
    searchEntities(query: string, options: FullTextSearchOptions): Promise<SearchCandidate[]>;
    /**
     * Full-text search on edges using PostgreSQL's text search
     */
    searchEdges(query: string, options: FullTextSearchOptions): Promise<SearchCandidate[]>;
    /**
     * Full-text search on episodes
     */
    searchEpisodes(query: string, options: FullTextSearchOptions): Promise<SearchCandidate[]>;
    /**
     * Search communities using full-text search
     */
    searchCommunities(query: string, options: FullTextSearchOptions): Promise<SearchCandidate[]>;
    /**
     * Search across all content types using full-text search
     */
    searchAll(query: string, options: FullTextSearchOptions & {
        includeEntities?: boolean;
        includeEdges?: boolean;
        includeEpisodes?: boolean;
        includeCommunities?: boolean;
    }): Promise<SearchCandidate[]>;
    /**
     * Enhanced search that handles different query types
     */
    smartSearch(query: string, options: FullTextSearchOptions): Promise<SearchCandidate[]>;
}
//# sourceMappingURL=fulltext-search.d.ts.map