import { SimpleOpenAIClient } from '../infrastructure/llm/simple-openai-client';
import type { SearchConfig, SearchResult, SearchFilters } from './search-config';
export interface HybridSearchOptions {
    config?: SearchConfig;
    filters?: SearchFilters;
    enableVector?: boolean;
    enableFullText?: boolean;
    enableReranking?: boolean;
}
export declare class HybridSearchEngine {
    private llmClient;
    private vectorSearch;
    private fullTextSearch;
    private bfsSearch;
    private reranker;
    constructor(llmClient: SimpleOpenAIClient);
    /**
     * Main hybrid search method combining vector, full-text, and BFS search
     */
    search(query: string, options?: HybridSearchOptions): Promise<SearchResult[]>;
    /**
     * Perform vector similarity search across configured content types
     */
    private performVectorSearch;
    /**
     * Perform full-text search across configured content types
     */
    private performFullTextSearch;
    /**
     * Perform BFS graph traversal search
     */
    private performBFSSearch;
    /**
     * Quick search with smart defaults
     */
    quickSearch(query: string, groupIds?: string[], limit?: number): Promise<SearchResult[]>;
    /**
     * Story-focused search optimized for writing contexts
     */
    storySearch(query: string, groupIds?: string[], centerNodeId?: string, limit?: number): Promise<SearchResult[]>;
    /**
     * Character-focused search using graph traversal
     */
    characterSearch(characterName: string, groupIds?: string[], limit?: number): Promise<SearchResult[]>;
    /**
     * Entity neighborhood exploration - showcase BFS + node distance reranking
     */
    exploreEntityNeighborhood(entityId: string, maxDepth?: number, limit?: number, groupIds?: string[]): Promise<{
        entities: SearchResult[];
        edges: SearchResult[];
        subgraph: {
            nodes: any[];
            edges: any[];
        };
    }>;
    /**
     * Find shortest path between two entities
     */
    findShortestPath(sourceEntityId: string, targetEntityId: string, maxDepth?: number, groupIds?: string[]): Promise<{
        path: any[];
        edges: any[];
        distance: number;
    } | null>;
    private getRerankingOptions;
    private toSearchResult;
}
//# sourceMappingURL=hybrid-search.d.ts.map