export interface BatchingMetrics {
    actualTokens: number;
    targetTokens: number;
    chunkSize: number;
    processingTime: number;
    success: boolean;
    errorMessage?: string;
}
export interface BatchingConfig {
    targetTokens: number;
    minChunkSize: number;
    maxChunkSize: number;
    adjustmentFactor: number;
    maxHistory: number;
}
export interface BatchProcessor<T, R> {
    (batch: T[]): Promise<R>;
}
export declare class AdaptiveBatcher<T, R> {
    private chunkSizeHistory;
    private currentChunkSize;
    private config;
    constructor(baseChunkSize?: number, config?: Partial<BatchingConfig>);
    /**
     * Process items in adaptive batches
     */
    batchProcess(items: T[], processor: BatchProcessor<T, R>): Promise<R[]>;
    /**
     * Create chunks from items
     */
    private createChunks;
    /**
     * Record metrics for analysis
     */
    private recordMetrics;
    /**
     * Adjust chunk size based on performance metrics
     */
    private adjustChunkSize;
    /**
     * Estimate token count for a chunk
     */
    private estimateTokens;
    /**
     * Get current batching statistics
     */
    getStats(): {
        currentChunkSize: number;
        averageProcessingTime: number;
        averageTokenRatio: number;
        successRate: number;
        totalBatches: number;
    };
    /**
     * Reset the batcher state
     */
    reset(newBaseChunkSize?: number): void;
}
//# sourceMappingURL=adaptive-batcher.d.ts.map