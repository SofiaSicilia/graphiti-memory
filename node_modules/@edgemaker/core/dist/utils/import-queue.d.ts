import type { DocumentChange } from '../types/whetstone';
import type { ImportedDoc, ImportManifest } from '../types/import-manifest';
/**
 * Project-scoped queue for processing imports sequentially
 * Ensures only one document processes per project at a time
 */
declare class ImportQueue {
    private queues;
    constructor();
    /**
     * Get or create a queue for a specific project
     * Each project gets its own queue with concurrency = 1
     */
    private getProjectQueue;
    /**
     * Enqueue a document for processing in the project-specific queue
     */
    enqueue<T>(projectId: string, task: () => Promise<T>): Promise<T>;
    /**
     * Clear all queues (for testing)
     */
    clear(): void;
    /**
     * Get active queue count for a project
     */
    getActiveCount(projectId: string): number;
    /**
     * Get pending queue count for a project
     */
    getPendingCount(projectId: string): number;
}
export declare const importQueue: ImportQueue;
/**
 * Helper to create fallback timestamp when writtenAt is missing
 */
export declare function createFallbackTimestamp(importedAt: string, sequence: number): Date;
/**
 * Build a DocumentChange from an ImportedDoc
 */
export declare function buildDocumentChange(doc: ImportedDoc, manifest: {
    projectId: string;
    importedAt: string;
}, userId: string, tiptapContent: any): DocumentChange;
/**
 * Create an import manifest from a list of documents
 */
export declare function createImportManifest(projectId: string, documents: Array<{
    id: string;
    title: string;
    path: string;
    writtenAt?: string;
    content?: any;
}>): ImportManifest;
export {};
//# sourceMappingURL=import-queue.d.ts.map