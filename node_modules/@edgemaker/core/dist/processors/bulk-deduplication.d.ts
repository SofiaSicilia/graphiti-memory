import type { EntityNode, EntityEdge } from '../types/core';
/**
 * Bulk deduplication system for import operations
 * Collects entities/edges in memory and processes in batches
 */
export declare class BulkDeduplicationProcessor {
    private entityMap;
    private edgeMap;
    private batchSize;
    private groupId;
    constructor(groupId: string, batchSize?: number);
    /**
     * Add entities to the in-memory collection
     */
    addEntities(entities: EntityNode[]): void;
    /**
     * Add edges to the in-memory collection
     */
    addEdges(edges: EntityEdge[]): void;
    /**
     * Check if we should flush based on batch size
     */
    shouldFlush(): boolean;
    /**
     * Flush accumulated entities and edges to database
     */
    flush(): Promise<void>;
    /**
     * Clear all accumulated data
     */
    clear(): void;
    /**
     * Get current counts
     */
    getCounts(): {
        entities: number;
        edges: number;
        total: number;
    };
    /**
     * Generate canonical key for entity deduplication
     */
    private getEntityKey;
    /**
     * Generate canonical key for edge deduplication
     */
    private getEdgeKey;
    /**
     * Merge two entities, preferring newer data
     */
    private mergeEntities;
    /**
     * Merge two edges, preferring newer data
     */
    private mergeEdges;
}
//# sourceMappingURL=bulk-deduplication.d.ts.map