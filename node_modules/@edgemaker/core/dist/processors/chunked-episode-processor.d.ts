import { EpisodeProcessor, EpisodeProcessingOptions, EpisodeProcessingResult } from './episode-processor';
import { ChunkingOptions } from './document-chunker';
import { SimpleOpenAIClient } from '../infrastructure/llm/simple-openai-client';
import type { EpisodeInput, EpisodeNode } from '../types/core';
export interface ChunkedEpisodeProcessingOptions extends EpisodeProcessingOptions {
    chunking?: ChunkingOptions;
    chunkDocuments?: boolean;
    minDocumentSizeForChunking?: number;
}
export interface ChunkedEpisodeProcessingResult extends EpisodeProcessingResult {
    chunks?: EpisodeNode[];
    edgeToChunkMapping?: Map<string, string[]>;
}
export declare class ChunkedEpisodeProcessor extends EpisodeProcessor {
    private documentChunker;
    private chunkEntityExtractor;
    private chunkEdgeExtractor;
    private readonly MIN_DOCUMENT_SIZE_FOR_CHUNKING;
    constructor(llmClient: SimpleOpenAIClient, defaultOptions?: Partial<ChunkedEpisodeProcessingOptions>);
    processEpisode(input: EpisodeInput, options?: ChunkedEpisodeProcessingOptions): Promise<ChunkedEpisodeProcessingResult>;
    private createChunkEpisodicEdges;
    private saveChunkedDataToDatabase;
}
//# sourceMappingURL=chunked-episode-processor.d.ts.map