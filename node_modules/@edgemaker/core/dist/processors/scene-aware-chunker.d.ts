export interface ChunkOptions {
    method: 'scene_aware' | 'paragraph' | 'fixed_size';
    maxChunkSize?: number;
    minChunkSize?: number;
    overlap?: number;
    preserveContext?: boolean;
}
export interface DocumentChunk {
    content: string;
    startChar: number;
    endChar: number;
    paragraphId: string;
    sceneId?: number;
    sequenceInDocument: number;
}
export declare class SceneAwareChunker {
    private static readonly SCENE_BREAK_PATTERNS;
    private static readonly PARAGRAPH_BREAK_PATTERN;
    private static readonly DEFAULT_OPTIONS;
    static chunkDocument(content: string, options?: Partial<ChunkOptions>): DocumentChunk[];
    private static sceneAwareChunk;
    private static identifyScenes;
    private static breakLargeScene;
    private static paragraphChunk;
    private static fixedSizeChunk;
}
//# sourceMappingURL=scene-aware-chunker.d.ts.map