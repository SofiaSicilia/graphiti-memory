import { DocumentProcessorV2, ImportedDoc } from './document-processor-v2';
export interface ImportOptions {
    concurrency?: number;
    resumeFromId?: string;
    validateBeforeImport?: boolean;
}
export interface ImportProgress {
    total: number;
    processed: number;
    succeeded: number;
    failed: number;
    currentDocumentId?: string;
    errors: Array<{
        documentId: string;
        error: string;
    }>;
}
export interface ImportResult {
    success: boolean;
    progress: ImportProgress;
    duration: number;
}
export declare class BatchDocumentImporter {
    private supabase;
    private documentProcessor;
    private progress;
    constructor(documentProcessor: DocumentProcessorV2);
    /**
     * Import multiple documents with parallel processing and progress tracking
     */
    importDocuments(documents: ImportedDoc[], groupId: string, options?: ImportOptions): Promise<ImportResult>;
    /**
     * Import a single document with error handling
     */
    private importSingleDocument;
    /**
     * Validate documents before import
     */
    private validateDocuments;
    /**
     * Check if a document already exists
     */
    private checkExistingDocument;
    /**
     * Create an import session for tracking
     */
    private createImportSession;
    /**
     * Update import session status
     */
    private updateImportSession;
    /**
     * Log progress for individual document import
     */
    private logImportProgress;
    /**
     * Reset progress tracking
     */
    private resetProgress;
    /**
     * Get current import progress
     */
    getProgress(): ImportProgress;
}
//# sourceMappingURL=batch-import-processor.d.ts.map