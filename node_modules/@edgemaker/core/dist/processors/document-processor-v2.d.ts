import { DocumentChunk } from './scene-aware-chunker';
import { SimpleOpenAIClient } from '../infrastructure/llm/simple-openai-client';
export interface ImportedDoc {
    document_id: string;
    content: string;
    metadata?: {
        title?: string;
        sequence?: number;
        [key: string]: any;
    };
}
export interface ProcessedDocument {
    document_id: string;
    title: string;
    sequence?: number;
    one_line_summary: string;
    paragraph_summary: string;
    scene_summaries: any;
    characters: string[];
    character_aliases: Record<string, string[]>;
    locations: string[];
    themes: string[];
    key_events: string[];
    word_count: number;
    document_embedding?: number[];
    chunks: ProcessedChunk[];
}
export interface ProcessedChunk extends DocumentChunk {
    content_embedding?: number[];
}
export declare class DocumentProcessorV2 {
    private llm;
    private supabase;
    constructor(llm: SimpleOpenAIClient);
    processDocument(doc: ImportedDoc, groupId: string, existingDoc?: any): Promise<ProcessedDocument>;
    private generateDocumentSummary;
    private generateEmbedding;
    private embedChunks;
    private generateEmbeddings;
    private saveNewDocument;
    private updateExistingDocument;
}
//# sourceMappingURL=document-processor-v2.d.ts.map