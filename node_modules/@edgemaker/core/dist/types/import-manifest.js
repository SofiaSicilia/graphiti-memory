"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ImportManifestSchema = void 0;
const zod_1 = require("zod");
// Import manifest schema for validation
exports.ImportManifestSchema = zod_1.z.object({
    projectId: zod_1.z.string().min(1),
    importedAt: zod_1.z.string().datetime(),
    documents: zod_1.z.array(zod_1.z.object({
        id: zod_1.z.string().min(1),
        title: zod_1.z.string(),
        path: zod_1.z.string(),
        sequence: zod_1.z.number().int().positive(),
        writtenAt: zod_1.z.string().datetime().optional()
    }))
}).refine((data) => {
    // Ensure sequence numbers are monotonic (no gaps allowed)
    const sequences = data.documents.map(doc => doc.sequence).sort((a, b) => a - b);
    for (let i = 0; i < sequences.length; i++) {
        if (sequences[i] !== i + 1) {
            return false;
        }
    }
    return true;
}, {
    message: "Sequence numbers must be monotonic starting from 1"
});
//# sourceMappingURL=import-manifest.js.map