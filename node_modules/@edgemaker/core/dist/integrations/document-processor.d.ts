import type { DocumentChange, DocumentMove, TipTapContent, TipTapProcessor, EntityMention } from '../types/whetstone';
import { WhetstoneBrain } from './whetstone-brain';
/**
 * Document change processor for Whetstone integration
 */
export declare class DocumentChangeProcessor implements TipTapProcessor {
    private brain;
    constructor(brain: WhetstoneBrain);
    /**
     * Process a document change and update the knowledge graph
     */
    processChange(change: DocumentChange): Promise<void>;
    /**
     * Process a document move operation
     */
    processMove(move: DocumentMove): Promise<void>;
    /**
     * Remove a document from the knowledge graph
     */
    removeDocument(documentId: string, userId: string): Promise<void>;
    /**
     * Extract plain text from TipTap content
     */
    extractText(content: TipTapContent): string;
    /**
     * Extract character names from dialogue in TipTap content
     */
    extractCharacters(content: TipTapContent): string[];
    /**
     * Detect scene breaks in TipTap content
     */
    detectSceneBreaks(content: TipTapContent): number[];
    /**
     * Calculate word count in TipTap content
     */
    calculateWordCount(content: TipTapContent): number;
    /**
     * Find mentions of specific entities in TipTap content
     */
    findEntityMentions(content: TipTapContent, entities: string[]): EntityMention[];
    private extractTextFromNode;
    private isLikelyCharacterName;
    private isSceneBreak;
    private escapeRegex;
    private extractContext;
    private calculateMentionConfidence;
}
/**
 * Factory function to create a document processor with a brain instance
 */
export declare function createDocumentProcessor(brain: WhetstoneBrain): DocumentChangeProcessor;
/**
 * Helper function to create a document change from TipTap content comparison
 */
export declare function createDocumentChangeFromComparison(documentId: string, userId: string, oldContent: TipTapContent | undefined, newContent: TipTapContent, metadata?: {
    title?: string;
    projectId?: string;
    version?: number;
}): DocumentChange;
//# sourceMappingURL=document-processor.d.ts.map