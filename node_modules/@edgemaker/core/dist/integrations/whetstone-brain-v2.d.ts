import { EdgeMakerV2 } from '../core/edgemaker-v2';
import type { DocumentChange, DocumentMove, ChatEnhancementRequest, WhetstoneConfig } from '../types/whetstone';
import type { ImportManifest, ImportProgress } from '../types/import-manifest';
export interface DocumentSearchResult {
    id: string;
    documentId: string;
    documentTitle: string;
    content: string;
    relevanceScore: number;
    sceneId?: number;
    paragraphId?: string;
}
export interface CharacterInfo {
    character: string;
    appearsInDocuments: string[];
    aliases: string[];
}
export interface ChatContextV2 {
    relevantDocuments: DocumentSearchResult[];
    relevantCharacters: CharacterInfo[];
    contextSummary: string;
    suggestedTopics: string[];
}
export declare class WhetstoneBrainV2 {
    private config;
    constructor(config: WhetstoneConfig & {
        openaiApiKey: string;
        openaiModel?: string;
        embeddingModel?: string;
    });
    processDocumentChange(change: DocumentChange): Promise<void>;
    processDocumentMove(move: DocumentMove): Promise<void>;
    removeDocument(documentId: string, projectId: string): Promise<void>;
    enhanceChat(request: ChatEnhancementRequest): Promise<ChatContextV2>;
    startImport(manifest: ImportManifest, userId: string): Promise<string>;
    enqueueBatch(sessionId: string, docs: any[], // DocumentWithContent from import-helpers
    manifest: ImportManifest, _userId: string, parseFile: (path: string) => any): Promise<void>;
    finishImport(sessionId: string): Promise<void>;
    cancelImport(sessionId: string): Promise<void>;
    pauseImport(sessionId: string): Promise<void>;
    resumeImport(sessionId: string): Promise<void>;
    enqueueDocument(sessionId: string, doc: any, manifest: ImportManifest, userId: string, parseFile: (path: string) => any): Promise<void>;
    getImportProgress(sessionId: string): Promise<ImportProgress>;
    clearProjectMemory(projectId: string): Promise<{
        success: boolean;
        deletedCounts: {
            documents: number;
            episodes: number;
            importSessions: number;
        };
        error?: string;
    }>;
    getEdgemakerForProjectId(projectId: string): EdgeMakerV2;
    private parseDocumentWithErrorHandling;
    private getImportSession;
    private updateImportProgress;
    private extractTextFromTipTap;
    private extractTextFromNode;
    private extractCharacterNames;
    private generateSuggestedTopics;
}
//# sourceMappingURL=whetstone-brain-v2.d.ts.map